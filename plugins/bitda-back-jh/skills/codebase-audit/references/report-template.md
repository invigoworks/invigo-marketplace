# Codebase Audit Report Template

감사 보고서 생성 시 이 템플릿을 사용한다.

---

```markdown
# Codebase Audit Report

**감사 일시**: {YYYY-MM-DD}
**감사 범위**: 전체 / {domain} 도메인
**검사 파일 수**: {N}개
**이전 감사**: {YYYY-MM-DD} (없으면 "없음")

---

## 요약

### 전체 평가

{종합적인 코드베이스 상태 평가 2-3문장}

### 영역별 현황

| 영역 | 발견 건수 | 심각 | 중간 | 낮음 |
|------|----------|------|------|------|
| 헌법 위반 | X건 | A | B | C |
| 시행령 위반 | Y건 | D | E | F |
| 패턴 불일치 | Z건 | G | H | I |
| 코드 중복 | W건 | J | K | L |
| 문서 동기화 | V건 | M | N | O |
| **합계** | **T건** | **P** | **Q** | **R** |

### 트렌드 (이전 감사 대비)

> 이전 감사가 없으면 이 섹션 생략

| 지표 | 이전 | 현재 | 변화 |
|------|------|------|------|
| 총 발견 건수 | X건 | Y건 | ✅ -N / ⚠️ +N |
| 헌법 위반 | A건 | B건 | ... |
| 시행령 위반 | C건 | D건 | ... |
| 패턴 불일치 | E건 | F건 | ... |
| 코드 중복 | G건 | H건 | ... |
| 문서 동기화 | I건 | J건 | ... |

---

## 1. 헌법 위반 (Constitution Violations)

### CLAUDE.md 준수 현황

| 규칙 | 상태 | 위반 건수 |
|------|------|----------|
| §2.2 가시성 규칙 (internal) | ✅/⚠️/❌ | N건 |
| §2.3 도메인 공유 규칙 | ✅/⚠️/❌ | N건 |
| §2.4 Zero-DTO 정책 | ✅/⚠️/❌ | N건 |
| §3.1 계층별 패키지 배치 | ✅/⚠️/❌ | N건 |
| §3.2 Double Model 전략 | ✅/⚠️/❌ | N건 |

### 발견 사항

> **[심각도]** `파일경로:라인` — `클래스/함수명`
> ```kotlin
> // 위반 코드 스니펫 (3-5줄)
> ```
> **위반 규칙**: CLAUDE.md §X.X
> **제안**: 수정 방향

{발견 사항이 없으면 "발견 사항 없음" 명시}

---

## 2. 시행령 위반 (Policy Violations)

### 시행령 준수 현황

| 시행령 | 상태 | 위반 건수 |
|--------|------|----------|
| temporal-data-policy.md | ✅/⚠️/❌ | N건 |
| validation-exception-policy.md | ✅/⚠️/❌ | N건 |
| query-pattern.md | ✅/⚠️/❌ | N건 |
| messaging-policy.md | ✅/⚠️/❌ | N건 |
| db-migration-policy.md | ✅/⚠️/❌ | N건 |
| test-infrastructure-spec.md | ✅/⚠️/❌ | N건 |
| excel-export-policy.md | ✅/⚠️/❌ | N건 |
| audit-logging-policy.md | ✅/⚠️/❌ | N건 |

### 발견 사항

> **[심각도]** `파일경로:라인` — `클래스/함수명`
> ```kotlin
> // 위반 코드 스니펫
> ```
> **위반 시행령**: {문서명} - {규칙}
> **제안**: 수정 방향

{발견 사항이 없으면 "발견 사항 없음" 명시}

---

## 3. 패턴 불일치 (Pattern Inconsistency)

### 패턴 분석 결과

| 계층 | 다수 패턴 | 소수 패턴 | 불일치 건수 |
|------|----------|----------|------------|
| Service 네이밍 | `{Action}{Domain}Service` (N개) | `{Domain}{Action}Service` (M개) | M건 |
| UseCase 반환값 | `UUID` (N개) | `Entity` (M개) | M건 |
| Mapper 함수명 | `toDomain()` (N개) | `toEntity()` (M개) | M건 |

### 발견 사항

> **[심각도]** 패턴 불일치: {설명}
> | 다수 패턴 (기대) | 소수 패턴 (실제) |
> |-----------------|-----------------|
> | `CreateUserService` | `UserCreateService` |
>
> **불일치 파일**: `파일1.kt`, `파일2.kt`
> **제안**: {다수 패턴으로 통일 권장}

{발견 사항이 없으면 "발견 사항 없음" 명시}

---

## 4. 코드 중복 (Duplications)

### 중복 유형별 현황

| 유형 | 발견 건수 |
|------|----------|
| Mapper 로직 | N건 |
| 검증 로직 | N건 |
| 쿼리 조건 | N건 |
| 예외 처리 | N건 |

### 발견 사항

> **[심각도]** 중복 유형: {유형}
> **관련 파일**:
> - `파일1.kt:라인` — `함수명()`
> - `파일2.kt:라인` — `함수명()`
> - `파일3.kt:라인` — `함수명()`
>
> **중복 패턴**:
> ```kotlin
> // 반복되는 코드 패턴
> ```
> **제안**: {공통 추출 방안}

{발견 사항이 없으면 "발견 사항 없음" 명시}

---

## 5. 문서 동기화 (Doc Sync Issues)

### 동기화 현황

| 유형 | 발견 건수 |
|------|----------|
| 주석 불일치 | N건 |
| MD 문서 불일치 | N건 |
| 깨진 참조 | N건 |
| 최신화 필요 | N건 |

### 주석 불일치

> **[심각도]** `파일경로:라인` — `함수명()`
> | 구분 | 내용 |
> |------|------|
> | 주석 | {KDoc 설명} |
> | 실제 | {함수 시그니처} |
>
> **제안**: 주석 업데이트

### MD 문서 불일치

> **[심각도]** `문서경로` — {섹션명}
> | 구분 | 내용 |
> |------|------|
> | 문서 | {문서 내용} |
> | 실제 | {코드 상태} |
>
> **제안**: 문서 업데이트

### 깨진 참조

| # | 문서 | 참조 | 상태 |
|---|------|------|------|
| 1 | `문서경로` | `참조 경로/클래스` | ❌ 존재하지 않음 |

### 최신화 필요

| # | 문서 | 신호 | 제안 |
|---|------|------|------|
| 1 | `문서경로` | TODO 주석 발견 | 업데이트 필요 |
| 2 | `문서경로` | 마지막 업데이트 > 6개월 | 검토 필요 |

{발견 사항이 없으면 "발견 사항 없음" 명시}

---

## 조치 필요 항목

| # | 심각도 | 영역 | 위치 | 내용 | 상태 |
|---|--------|------|------|------|------|
| 1 | 심각 | 헌법위반 | `파일:라인` | 설명 | ⬜ |
| 2 | 중간 | 시행령위반 | `파일:라인` | 설명 | ⬜ |
| 3 | 낮음 | 패턴불일치 | `파일:라인` | 설명 | ⬜ |

> 영역: 헌법위반, 시행령위반, 패턴불일치, 코드중복, 문서동기화

---

## 권장 우선순위

### 즉시 조치 (심각)

1. {심각 항목 1}
2. {심각 항목 2}

### 단기 조치 (중간)

1. {중간 항목 1}
2. {중간 항목 2}

### 장기 개선 (낮음)

1. {낮음 항목 1}
2. {낮음 항목 2}

---

🤖 Audited by [Claude Code](https://claude.com/claude-code)
```
